// home.js — bosh sahifa uchun qo'shimcha interaktivlik (kelajakda kengaytirish uchun joy)

$(function () {
  // Hozircha bosh sahifa serverda to'liq render qilinadi (SSR).
  // Agar kelajakda "eng ko'p ijaraga olingan kitoblar" kabi dinamik bloklar
  // qo'shilsa, shu yerdan /api/products ga so'rov yuborilishi mumkin.
});
